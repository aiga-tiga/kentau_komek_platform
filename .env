# Copy to .env in this same folder (next to docker-compose.yml) before running
# `docker compose up -d --build`.

JWT_SECRET=change-this-to-a-long-random-string
TELEGRAM_BOT_TOKEN=8833731649:AAG1XsN3FyniQe6J6e26_npqaMTVxwglAR4
BOT_SHARED_SECRET=optional-shared-secret-between-bot-and-api

# Set this to the domain/IP where the frontend will be reachable, e.g.
# http://your-server-ip or https://complaints.yourdomain.kz
# Used to build absolute URLs to uploaded photos.
PUBLIC_BASE_URL=http://localhost:4000
