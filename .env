# Copy to .env in this same folder (next to docker-compose.yml) before running
# `docker compose up -d --build`.

JWT_SECRET=change-this-to-a-long-random-strings2026
TELEGRAM_BOT_TOKEN=8833731649:AAHd2SAo92nCQFDBr07WdqJVDKYKCK3LJTs
BOT_SHARED_SECRET=optional-shared-secret-between-bot-and-api

# Set this to the domain/IP where the frontend will be reachable, e.g.
# http://your-server-ip or https://complaints.yourdomain.kz
# Used to build the "check your complaint status" link sent by SMS.
PUBLIC_BASE_URL=http://localhost:4000

# smsc.kz credentials for the confirmation SMS (complaint code + access code).
# Leave blank to disable SMS sending - see backend/sms.js.
